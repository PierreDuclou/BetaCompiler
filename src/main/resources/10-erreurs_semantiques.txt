// 10. Programme qui co ntient des erreurs sémantiques
function add(int a, int b): int {
    return a + b;
}


function main(): void {
    undefined = 123;        // erreur de variable non définie
    
    int a;
    int a = 2;              // erreur de double définition

    int b = add(12, 2, 123) // erreur de nombre de paramètres

    int c;
    c = true;               // erreur de compatibilité de types
}